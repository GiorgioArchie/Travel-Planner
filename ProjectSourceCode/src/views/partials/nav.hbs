<header>
  <nav class="navbar navbar-expand-sm navbar-light bg-light shadow-sm border-bottom">
    <div class="container">
      <span class="navbar-brand fw-bold">
        🌍 Travel Planner
      </span>

      <button
        class="navbar-toggler"
        type="button"
        data-bs-toggle="collapse"
        data-bs-target="#navbarNav"
        aria-controls="navbarNav"
        aria-expanded="false"
        aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav me-auto mb-2 mb-sm-0">
          <li class="nav-item">
            <a class="nav-link" href="{{#if LoggedIn}}/map{{else}}/login{{/if}}">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="{{#if LoggedIn}}/calendar{{else}}/login{{/if}}">Calendar</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="{{#if LoggedIn}}/trips{{else}}/login{{/if}}">All Trips</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="{{#if LoggedIn}}/journal{{else}}/login{{/if}}">Journal</a>
          </li>
        </ul>

        {{#if LoggedIn}}
        <div class="d-flex align-items-center">
          <span class="me-3 text-muted small">Hello, {{username}}</span>
          <a href="/logout" class="btn btn-outline-danger btn-sm">Logout</a>
        </div>
        {{/if}}
      </div>
    </div>
  </nav>
</header>
